############## 达梦数据库配置 ##############
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:dm://220.196.89.35:3000/ZY_PLATFORM?serverTimezone=GMT%2B8&&characterEncoding=utf8&useSSL=false
    username: ZY_PLATFORM
    password: ZY_PLATFORM@16_justai1033!
    druid:
      initial-size: 2
      min-idle: 2
      max-active: 5
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      filters: stat,slf4j
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000

  ai:
    dashscope:
      api-key: sk-ffe8edfe3d7e46f09d538a7a8a9e1913
      model: Qwen3-Max
      embedding-model: text-embedding-v4
      dimensions: 1536
  data:
    redis:
      host: 220.196.89.35
      port: 3005
      password: redis_19_justai1033
      database: 1
      timeout: 10s
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

############## 日志配置 ##############
logging:
  level:
    com.zy.abletogetherai.mapper: debug
    cn.dev33.satoken: debug

milvus:
  enabled: false
  host: 220.196.89.35
  port: 5243
  database: zy_platform

# MinIO对象存储配置
# 配置你的远程MinIO服务器信息
storage:
  type: minio
  endpoint: http://220.196.89.54:49161  # 替换为你的远程MinIO地址
  access-key: wsupport               # 替换为你的MinIO Access Key
  secret-key: Justai1033@13               # 替换为你的MinIO Secret Key
  bucket-name: zr-platform                  # 替换为你的Bucket名称
  secure: false                             # 如果使用HTTPS则设为true

external:
  call:
    # 外部外呼接口URL https://api.q.52store.com/api/app/reportCall
    url: http://220.196.89.32:4110

# 高德地图配置（请替换为有效的API Key）
# key 和 securityConfig 给前端使用
# webKey 给后端Web服务使用（地理编码等）
# 获取地址：https://console.amap.com/dev/key/app
amap:
  key: ${AMAP_KEY:a0e376b4b78219135989c322f81b9ef8}
  securityConfig: ${AMAP_SECURITY_CONFIG:c9cdb1e7314ce353f3573610415fea0b}
  webKey: ${AMAP_WEB_KEY:6640d92bf13e62c5ef4421321f7a7cb1}
  enabled: ${AMAP_ENABLED:true}
